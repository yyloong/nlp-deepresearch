runs/
runs_distill/
distill/

# API secrets — copy secrets.json.example -> secrets.json and fill in
secrets.json

# Local BM25 / SQLite indexes (often >100MB; do not push to GitHub)
indexes/*.sqlite
indexes/*.sqlite-shm
indexes/*.sqlite-wal

__pycache__/
*.pyc
agent/__pycache__/

# SFT 训练数据（原始 + 清洗后 + 压缩后）
train/sft_data.jsonl
train/sft_data_condensed*.jsonl
train/sft_data_distill.jsonl
train/*.token_counts.json
train/sample_condensed*.json